function backpopulate_mask(blk,mv)
% Back populate a block's mask's values.
%
% backpopulate_mask( blk, mv )
%
% blk - The block whose mask will be dumbed down
% mv - The MaskValue cell array of strings to back populate

  % Back populate mask parameter values
  set_param(blk,'MaskValues',mv);
